import { Client, EmbedBuilder, Message } from "discord.js";

export const data = {
  name: "searchDone",
  distube: true
}

export async function execute(message: Message, answer: Message, query: String) {

}
